<%= form_with(model: doing_list, local: true) do |form| %>
  <% if doing_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doing_list.errors.count, "error") %> prohibited this doing_list from being saved:</h2>

      <ul>
      <% doing_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :doing_list_user_id %>
  </div>

  <div class="field">
    <%= form.label :tweet_id %>
    <%= form.number_field :tweet_id, id: :doing_list_tweet_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
