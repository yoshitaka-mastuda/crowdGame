<br><br>

<%= link_to '戻る', home_index_path %>
<br><br>

<h1>非承認ツイートの一覧</h1>
<p>　ツイートをクリックすると不適切だと判断された理由が見れます。</p>
<table class="table table-striped table-bordered">
  <thead>
  <tr>
    <th>あなたが投稿したツイート</th>
  </tr>
  </thead>

  <tbody>
  <% @reject_tweets.each do |a_t| %>
      <tr>
          <td><%= link_to tweet_insert_reason_path(t_id: a_t.tweet_id) do %><%= a_t.text %><% end %></td>
  <% end %>
  </tbody>
</table>
<br>

<h1>承認ツイートの一覧</h1>
<table class="table table-striped table-bordered">
  <thead>
  <tr>
    <th>あなたが投稿したツイート</th>
  </tr>
  </thead>

  <tbody>
  <% @accept_tweets.each do |a_t| %>
      <tr>
        <td><%= a_t.text %></td>
      </tr>
  <% end %>
  </tbody>
</table>
<br>

