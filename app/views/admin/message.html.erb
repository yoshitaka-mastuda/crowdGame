<br><br>

<%= link_to '戻る', admin_tweet_path %>
<br><br>




<h3>不適切だと判断された理由一覧</h3>
<table class="table table-striped table-bordered">
  <thead>
  <tr>
    <th>理由</th>
    <th>ツイートID</th>
    <th>URL</th>
  </tr>
  </thead>
  <tbody>
  <% @reason.each do |r| %>
      <tr>
        <td><%= r.message %></td>
        <td><%= r.tweet_id %></td>
        <td><%= link_to 'リンク', admin_tweet_url_path(tweet_id: r.tweet_id) %></td>
      </tr>
  <% end %>
  </tbody>
</table>
