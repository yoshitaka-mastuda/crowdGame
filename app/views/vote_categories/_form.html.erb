<%= form_with(model: vote_category, local: true) do |form| %>
  <% if vote_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vote_category.errors.count, "error") %> prohibited this vote_category from being saved:</h2>

      <ul>
      <% vote_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vote_id %>
    <%= form.number_field :vote_id, id: :vote_category_vote_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id, id: :vote_category_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
